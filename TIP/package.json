{
  "name": "telemaster-intelligence-platform",
  "private": true,
  "version": "0.1.0",
  "description": "Local-first internal lead intelligence platform foundation for TeleMaster India.",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "dev:frontend": "npm --workspace frontend run dev",
    "dev:backend": "npm --workspace backend run dev",
    "db:migrate": "npm --workspace backend run db:migrate"
  },
  "workspaces": [
    "frontend",
    "backend"
  ],
  "engines": {
    "node": ">=20"
  }
}
